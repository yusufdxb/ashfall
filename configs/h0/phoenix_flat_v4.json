{
  "_comment": "Phoenix backend for H0 on the Flat-v0 v4 policy. ${PHOENIX_ROOT} is expanded by the CLI; set it to the go2-phoenix checkout. Paths are relative to that checkout so no author filesystem leaks into the repo.",
  "checkpoint": "${PHOENIX_ROOT}/checkpoints/phoenix-flat-v4/latest.pt",
  "env_config": "${PHOENIX_ROOT}/configs/env/flat_v4.yaml",
  "train_config": "${PHOENIX_ROOT}/configs/train/ppo_flat_v4.yaml",
  "device": "cuda:0",
  "horizon_s": 10.0,
  "phoenix_repo": "${PHOENIX_ROOT}",
  "simulator_version": "recorded_from_environment_snapshot"
}

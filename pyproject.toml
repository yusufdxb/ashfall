[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ashfall"
version = "0.2.0"
description = "Hardware-counterexample-guided policy repair for legged robots"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [{name = "Yusuf Guenena", email = "yusuf.a.guenena@gmail.com"}]

dependencies = [
    "numpy>=1.24",
    "pyarrow>=14.0",
    "pyyaml>=6.0",
    "omegaconf>=2.3",
    "matplotlib>=3.7",
    "scipy>=1.11",
    "rich>=13.0",
    "tabulate>=0.9",
]

[project.scripts]
ashfall = "ashfall.cli:main"

[project.optional-dependencies]
sim = [
    "torch>=2.1",
    "tensorboard",
]
dev = [
    "pytest>=7.0",
    "pytest-cov",
    "hypothesis>=6.0",
    "ruff",
    "black",
    "mypy",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "sim: requires Isaac Lab (deselect with -m 'not sim')",
    "guard: scientific guard that must fail when its protected defect is reintroduced",
]

[tool.mypy]
python_version = "3.10"
ignore_missing_imports = true
warn_unused_ignores = false
# Typed core. Legacy analysis modules are checked as they are migrated.
files = [
    "src/ashfall/ontology.py",
    "src/ashfall/provenance.py",
    "src/ashfall/backends/phoenix_compat.py",
]

[tool.black]
line-length = 100
target-version = ["py310"]
